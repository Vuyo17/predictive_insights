{
  "name": "submission-metrics-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "refresh": "node scripts/build-submission-metrics.mjs",
    "refresh:watch": "node scripts/build-submission-metrics.mjs --watch",
    "dev": "npm run refresh && vite",
    "build": "npm run refresh && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "@vitejs/plugin-react": "^4.3.2"
  }
}
